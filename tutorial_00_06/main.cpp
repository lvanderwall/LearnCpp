#include <iostream>     // for std::cout (standard output) and std::endl

int main()              // every program needs to have a function "int main()"
{
    std::cout << "Hello world!" << std::endl;
    return 0;
}
